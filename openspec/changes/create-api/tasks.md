# Tasks

## Completed
- [x] Implement Express server and middleware (cors, json)
- [x] Implement endpoints: GET /health, POST /api/repos, GET /api/repos, GET /api/repos/:id
- [x] Configure pg Pool using DATABASE_URL + ssl rejectUnauthorized=false

## Remaining
- [x] Add SQL migration file (schema for repos, analysis_jobs, detected_components, evidence)
- [x] Add README with curl examples + local run steps
- [x] Add basic startup DB connectivity log (optional)